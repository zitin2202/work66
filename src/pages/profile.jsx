import React from 'react';
import ProfileMain from "../components/profileMain";

const Profile = () => {
    return (
        <div>
            <ProfileMain/>
        </div>
    );
};

export default Profile;